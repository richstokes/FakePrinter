
# Fake Printer

Creates a fake/virtual printer on the network. Receives print jobs and writes them to a PDF file under `./print_jobs/`.


## Install
`pipenv install`
`brew install ghostscript` (if you want PDF output)


## Run
`pipenv run python fakeprinter.py`
