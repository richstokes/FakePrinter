
# Fake Printer

Spoofs a fake/virtual printer on the network. Receives print jobs and writes them to a PDF file under `./print_jobs/`.


## Install
`pipenv install`


## Run
`pipenv run python fakeprinter.py`